services:
  - type: web
    name: cloud-reverse-proxy-extractor
    env: node
    plan: free
    buildCommand: ""
    startCommand: "node server.js"
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      - key: TARGET_URL
        sync: false
